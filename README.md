# 特征工程.ipynb文件的结构如下：

```python
1，数据质量检测
    1.1，数据合并
        1.1.1，加载数据
        1.1.2，堆叠合并
        1.1.3，主键合并
    1.2，数据特征
    	1.2.1，数值型类别型时间型特征
        1.2.2，数值型特征查看统计摘要
        1.2.3，类别型特征查看频数
        1.2.4，时间型特征字符串转为时间
    1.3，数据预处理
    	1.3.1，删除重复值
        1.3.2，填补缺失值
        1.3.3，修改异常值
        	1.3.3.1，箱线图判断异常值
                1.3.3.2，判断完异常值用四分位数进行替换
                1.3.3.3，3西格玛原则判断异常值
                1.3.3.4，判断完异常值用边界值替换
    1.4，数据标准化
    	1.4.1，归一标准化
        1.4.2，离差标准化
        1.4.3，Z-score标准化
    1.5，数据类别不平衡处理
2，特征工程
    2.1，特征选择
    	2.1.1，相关性分析
        2.1.2，关联度分析
    2.2，特征提取
        2.2.1，随机森林
        2.2.2，主成分分析
```

# 聚类.ipynb文件的结构如下：

```python
1，KMeans
    1.1，加载数据集
    1.2，聚类结果
    1.3，轮廓图
    1.4，决策边界
2，GMM
    2.1，加载数据集
    2.2，决策边界
    2.3，集群形状的选择
    2.4，异常检测
    2.5，选择聚类数
3，DBSCAN
    3.1，加载数据集
    3.2，可视化核心点、边界点和噪声点
4，KMeans
    4.1，加载数据集
    4.2，绘制层次聚类树
```

# 分类.ipynb文件的结构如下：

```python
1，划分训练测试验证集
2，KNN
    2.1，验证集调节超参数
    2.2，分类结果和精度
    2.3，评价指标
3，朴素贝叶斯
    3.1，分类结果和精度
    3.2，评价指标
4，支持向量机
    4.1，网格搜索超参数
    4.2，分类结果和精度
    4.3，评价指标
5，决策树
    5.1，网格搜索超参数
    5.2，分类结果和精度
    5.3，评价指标
    5.4，可视化决策树
    5.5，特征重要性
    5.6，二维决策边界
6，集成学习
    6.1，XGB
        6.1.1，网格搜索超参数
        6.1.2，分类结果和精度
        6.1.3，评价指标
        6.1.4，特征重要性
    6.2，随机森林
    	6.2.1，网格搜索超参数
        6.2.2，分类结果和精度
        6.2.3，评价指标
        6.2.4，特征重要性
    6.3, 以随机森林为例保存模型
7，logistic回归
    7.1，一维决策边界
    7.2，二维决策边界
8，softmax回归
```

# 回归.ipynb文件的结构如下：

```python
1，划分训练测试验证集
2，线性回归
3，岭回归
4，lasso回归
5，支持向量机回归
6，决策树回归
7，XGB回归
8，随机森林回归
```

